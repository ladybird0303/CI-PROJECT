// App.tsx — Expo Router handles all navigation via the app/ directory.
// This file exists as a fallback entry point; the actual entry is index.ts → expo-router/entry.
export { default } from 'expo-router/build/qualified-entry';
